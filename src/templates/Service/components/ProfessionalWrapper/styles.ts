import styled from 'styled-components'

export const Wrapper = styled.section`
  h2 {
    color: var(--blackTwo);
    font-family: var(--title);
    font-weight: 700;
    font-size: 35px;
    line-height: 44px;
    margin-bottom: 47px;
  }
`
