import styled from 'styled-components'

export const History = styled.main`
  padding-top: 90px;
  padding-bottom: 30px;

  h1 {
    color: var(--brownTwo);
    font-family: var(--title);
    font-weight: 700;
    font-size: 72px;
    line-height: 90px;
    text-align: center;
    margin-top: 30px;
  }
`
